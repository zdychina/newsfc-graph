---
id: "{{nf}}@AtomTask@{{command}}"
type: "AtomTask"
name: "{{command}}"
name_zh: "{{配置动作名}}"
nf: "{{nf}}"
ref: "{{nf}}@MMLCommand@{{command}}"
status: "draft"
---

# {{配置动作名}}（{{command}}）

> 命令静态知识（功能/参数定义/规格）见 [[{{nf}}@MMLCommand@{{command}}]]。
> 本页只讲配置时这条命令**怎么配**：按配置维度组织成字典，不逐特性、不逐运营商罗列取值。

## 配置方法

{{1-2 段：命令做什么 + 核心配置维度有哪些}}

### 配置维度 1：{{维度名}}（参数 {{PARAM}}）

| 取值 | 作用 | 配套必选参数 | 典型场景 |
|---|---|---|---|
| {{取值}} | {{作用}} | {{配套参数或"无"}} | {{场景}} |

> 多维度时每维度一个 `### 配置维度 N` 子节；最少保留"取值 / 作用"两列。

## 决策点

| 选项 | 影响（联动参数/场景） |
|---|---|
| {{选项}} | {{影响}} |

> 用法单一时删除表格，写：本命令用法单一，无分支。

## 约束

- **{{规则名}}**（{{critical|warning|info}}）：{{约束}} — {{违反后果}}

### 引用约束

| 参数 | 只能引用 | 说明 |
|---|---|---|
| {{PARAM}} | `{{ADD XXX}}` 创建的 {{对象}} | {{须先执行 / 取值一致性}} |

> 无引用型参数时删除表格，写：本命令无引用型参数。

## 边
- 对应命令: [[{{nf}}@MMLCommand@{{command}}]]
