---
id: "ConfigurationSolution@{{scenario}}-{{solution}}"
type: "ConfigurationSolution"
name: "{{方案中文名}}"
name_zh: "{{方案中文名}}"
domain: "{{domain}}"
scenario: "{{scenario}}"
operator: "{{operator}}"
status: "draft"
---

# {{方案中文名}}

> {{一句话：这个方案怎么配}}。属于 [[NetworkScenario@{{scenario}}]]。
> 依据：《{{文档名}}》（{{文档版本}}）；构建时平台命令版本 {{版本}}。

## 概览

{{方案解决什么 + 由哪些单元组成 + 协同骨架，1-2 段}}

## 配置与协同

**特性关系矩阵**（必填；不编排特性时写"本方案不编排产品特性"）：

| 特性 | 角色 | 必配? | 关系说明 |
|---|---|---|---|
| [[{{nf}}@FeatureTask@{{feature_code}}]] | 核心 / 基础(依赖前提) / 维度增强 / 跨网元对端 / 二选一 | 必配 / 可选(条件) | {{重叠 / 正交 / 组合 / 包含}} |

### 配置编排

> 按语句级时间线的执行顺序。每行一个编排单元，修饰写在括号里，语法见 对象规范.md §6：
> `条件：` / `激活方法：`（引用 FT 必填）/ `省略：步骤 n` / `后移：步骤 n → 第 m 行` / `仅步骤：a–b` / `重复：按 <维度>` / `增量：在第 n 行基础上`

1. **{{单元名}}**：[[{{nf}}@AtomTask@{{CMD}}]]（条件：{{条件}}）
2. **{{单元名}}**：[[{{nf}}@CompoundTask@{{slug}}]]（重复：按 {{维度}}）
3. **{{单元名}}**：[[{{nf}}@FeatureTask@{{feature_code}}]]（激活方法：{{FT 激活方法名}}；后移：步骤 {{n}} → 第 4 行）
4. **{{单元名}}**：[[{{nf}}@AtomTask@{{CMD}}]]（[[{{nf}}@FeatureTask@{{feature_code}}]] 的步骤 {{n}}）

### {{单元名}}：变种与排除

{{相对标准配置方法的差异点 + 原因；无则写"走标准配置方法，无变种"}}

### 跨单元 / 跨网元协同

{{先后依赖、需两端对齐的参数、变更联动}}

## 运营商规范

> 仅 operator 方案必填。每条注明作用的命令与参数；局点数据已脱敏。

### 强制项

- **{{规则名}}**：`{{CMD}}` 的 `{{PARAM}}` 必须为 `{{值}}` — {{原因/后果}}

### 推荐值

| 命令 | 参数 | 推荐值 | 说明 |
|---|---|---|---|
| `{{CMD}}` | `{{PARAM}}` | `{{值}}` | {{可按实际调整的范围}} |

### 规划规则

- `{{PARAM}}`：{{命名 / 编号 / 取值规则}}

### 成员与对应关系

| 对象 | 规则 / 清单 | 作用的命令与参数 |
|---|---|---|
| {{组 / 号段 / 对端}} | {{成员构成、号段 → 取值、适用的对端}} | `{{CMD}}` `{{PARAM}}` |

## 决策点

### DP1：{{决策点名}}

| 选项/场景 | 影响（参数/命令/联动） |
|---|---|
| {{选项}} | {{走哪个 Task / 跳过哪步 / 关键参数}} |

## 约束

- **{{规则名}}**（{{critical|warning|info}}）：{{约束}} — {{违反后果}}

## 边
- 上游场景: [[NetworkScenario@{{scenario}}]]
- 编排特性: [[{{nf}}@FeatureTask@{{feature_code}}]]
- 复用步骤: [[{{nf}}@CompoundTask@{{slug}}]]
- 复用命令: [[{{nf}}@AtomTask@{{CMD}}]]
