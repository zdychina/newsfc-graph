---
id: "{{nf}}@CompoundTask@{{slug}}"
type: "CompoundTask"
name: "{{slug}}"
name_zh: "{{步骤中文名}}"
nf: "{{nf}}"
command_set: ["{{CMD1}}", "{{CMD2}}", "{{CMD3}}"]
status: "draft"
---

# {{步骤中文名}}

> {{定位一句话：本步骤完成什么配置目标，为谁提供基础}}。

## 配置方法

{{引子 1-2 句}}

| 步骤 | 命令 | 关键参数 |
|---|---|---|
| {{步骤名}} | `{{CMD1}}` → [[{{nf}}@AtomTask@{{CMD1}}]] | `{{参数}}`=`<规划值>` |
| {{步骤名}} | `{{CMD2}}` → [[{{nf}}@AtomTask@{{CMD2}}]] | `{{参数}}`=`<规划值>`（引用步骤 1 的 {{对象}}） |

**典型脚本**（基线只写 `<规划值>` 和对所有引用方都成立的取值；单一来源的具体取值写进场景差异，局点数据已替换为占位）：

```
{{CMD1}}: {{P1}}=<规划值>, {{P2}}={{枚举值}};
{{CMD2}}: {{P3}}=<引用步骤1对象>;
```

**步骤位置**：{{前置于哪些步骤 / 依赖哪些步骤}}

## 场景差异

> 每个引用方（FeatureTask 的一种激活方法，或 CS 的一个单元）一行；复用时必须回填本表（防假通用）。

| 引用方 / 场景 | 执行命令 / 省略命令 | 相对基线参数差异（参数=值） | 对象与步骤位置 |
|---|---|---|---|
| [[{{引用方ID}}]] / {{场景}} | {{执行（含重复次数）/ 省略}} | `{{参数}}`=`{{值}}`；无差异写"与基线相同" | {{对象、前后置}} |

## 决策点

| 选项/场景 | 影响（参数/命令/联动） |
|---|---|
| {{选项}} | {{影响}} |

> 无分支则写：本步骤用法单一，无分支。

## 约束

- **{{规则名}}**（{{critical|warning|info}}）：{{约束}} — {{违反后果}}

## 边
- 组成: [[{{nf}}@AtomTask@{{CMD1}}]], [[{{nf}}@AtomTask@{{CMD2}}]], [[{{nf}}@AtomTask@{{CMD3}}]]
